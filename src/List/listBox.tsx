import React from "react"
import styles from './style/listBox.less'

const ListBox = (children:any) => {
    return (
        <div className={styles.listBox}>

        </div>
    )
}

export default ListBox